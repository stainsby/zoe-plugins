## 1.4.0 → 1.4.1 (a work session does not open a management session by its own act; the worker brief's dispatch time is a clock reading)

**Files:** 16 files, **0 added**, **0 removed**, **2 changed** — `kernel/VERSION` and
`kernel/skills/zoe-run/SKILL.md`.

**Size:** 13570 → 13609 tokens (tiktoken `cl100k_base`), 59210 → 59354 bytes. Growth of +39
tokens: a rewritten *Hand off* paragraph and one added clause in the brief, both in
`kernel/skills/zoe-run/SKILL.md`. From this release the token count is the size measure;
earlier entries give lines / words / bytes.

**Adopter notes.**

1. **A work session no longer opens a management session by its own act.** The *Hand off*
   paragraph of `kernel/skills/zoe-run/SKILL.md` used to say that where an assessment is due,
   the results go to assess, "which makes this a management session" — and, otherwise, that
   the session ends with its records written and the next management session assesses them.
   The first branch let a work session turn itself into a management session without regard
   to who the index says may open one. It now says: the results go to assess in this session
   if it is already a management session, otherwise in the next one, which opens only as your
   index says; a work session does not open one by its own act. The second branch is
   unchanged in substance. Compare that paragraph first. The rule itself was already the
   instructions' (*The cycle*: assess runs only in a management session; *Terms*: your index
   lists the circumstances under which one can occur) — the hand-off now defers to it instead
   of contradicting it.
   **What to do:** nothing, unless one of your own skills cites `zoe-run`'s hand-off — re-read
   those against the new wording.
2. **The worker brief's dispatch time is a clock reading.** In the brief paragraph of
   `kernel/skills/zoe-run/SKILL.md`, "when it was dispatched" becomes "when it was dispatched,
   as a clock reading — a worker that cannot read a clock uses it as its time". One line added.
   **What to do:** nothing, unless your dispatch procedure types or rounds the time into a
   brief — paste a reading taken from an authoritative clock at the moment of dispatch instead
   (see *Tools* in the instructions).
